{% extends 'base.html.twig' %}

{% block body %}

<a class="btn btn-primary"
  href="/country/add">Ajouter un pays</a>

<table class="table table-striped table-bordered">
  {% for country in countries %}
    <tr>
      <td>{{ country.name }}</td>
      <td>
        <a href="/country/{{country.id}}/delete">supprimer</a>
      </td>
    </tr>
  {% endfor %}
</table>

{% endblock %}
